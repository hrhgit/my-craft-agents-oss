# Future TODO

> 本文档只记录未来可能实现的想法，不是当前项目需求、实施计划、验收标准或决策依据。
>
> 默认情况下，进行项目开发、排错、评审或规划时不要读取、引用或据此推断任务范围。只有当用户明确说要调用、参考或处理这份 TODO 文档时，才将其中内容纳入当前任务。

状态：未拆解，未排期。

## 2. 会话编号、跨会话查看与引用

- 为每个会话提供一个简单编号。
- 给模型提供快速查看其他会话内容的工具或其他等效方式。
- 允许把侧栏中的会话卡片拖入对话框，作为对其他会话内容的引用。
- 支持针对其他会话设置定时任务，例如某个会话完成后触发后续任务。

## 3. 语音输入

- 增加语音输入功能。

## 4. 工作模式隔离

- 增加模式功能，用于隔离不同的工作模式。
- 模式可能需要隔离系统提示词、上下文、工具、skill、子智能体等内容。
- 考虑扩展隔离，以及每种模式自定义 UI 样式的能力。

## 5. Agent / 智能体设置模块

- 在设置中增加 Agent 或“智能体”模块。
- 支持修改系统默认提示词。
- 支持配置系统可见的工具。
- 支持预设子智能体。
- 支持修改压缩上下文时使用的提示词。

## 6. 额外 workspace content

- 评估更多适合作为 `workspace.content` 的查看与编辑工具，让用户在 universal dock 中自行分组、移动和 detach。
- 右侧仅可作为用户布局后的结果，不恢复专用右侧内容架构、固定方向 surface 或 `workbench.right` 兼容层。

## 7. GUI 能力优先于斜杠命令

- 对已经在 Mortise GUI 中实现的能力，在用户输入 `/` 时屏蔽对应的斜杠命令，不再重复提供两套操作入口。
- 只有尚未提供 GUI 入口的能力，才继续通过斜杠命令操作。

## 8. Mortise 命名收敛

- 清理 Mortise 自有层和用户界面中残留的 `Pi` 命名，包括迁移已结束后仍存在的“迁移的 Pi 扩展”等过时文案。
- 将 Mortise 自有的 provider、extension、RPC、projection 和 UI 类型、文件及 API 名称收敛为 Mortise、Agent 或 Runtime 语义。
- 允许嵌入式 Pi Agent Loop 的适配边界暂时保留 `Pi` 实现名称；是否进一步重命名 `@mortise/pi-*`、`PI_CODING_AGENT_*` 和嵌入式 fork，需要单独评估协议、扩展 SDK 和打包影响。
- 该清理不得重新引入 `.pi` 数据路径、运行时兼容或双写。
