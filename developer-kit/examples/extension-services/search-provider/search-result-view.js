export function createSearchResultView() {
  const root = document.createElement('div')
  root.className = 'example-search-result-view'
  root.textContent = 'Search capability UI module loaded'
  return root
}
