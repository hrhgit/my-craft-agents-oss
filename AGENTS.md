# Mortise Agent Notes

## 最高优先级：产品语义讨论与沉淀

- 用户明确同意按产品语义讨论结果执行，即视为讨论结束。此时必须先完成纪要和已经达成结论的语义文档更新，再开始具体的代码、配置或其他实现改动；不得只留在对话、实现代码、测试或临时任务说明中。
- 每次这类讨论都必须在 [`docs/product-semantics-discussions/`](docs/product-semantics-discussions/README.md) 新建按日期命名的纪要，按时间顺序记录提出的观点、分歧或冲突、依据、最终共识及其落入的语义文档位置。未形成共识的问题必须明确标为待决定，不得伪装成既有产品语义。
- `docs/product-semantics.md` 是跨领域产品语义的总参考；专题语义应写入对应的专题文档。讨论纪要保存推导过程，不替代规范语义文档。

## 项目术语约定

- 在本仓库工作场景下，**Mortise 默认指源码层面**；即使你本地是使用安装版做开发，只要相关指令提到 Mortise，Agent 默认按**改源码**理解。若你明确想要只对安装版做运行时验证、而不改源码，需要单独说明。

## Runtime Diagnostics

- The primary local diagnostic log is `%MORTISE_CONFIG_DIR%\logs\runtime.log` when `MORTISE_CONFIG_DIR` is set, otherwise `%USERPROFILE%\.mortise\logs\runtime.log`.
- For `Pi Process Exited`, `get_capabilities`, or Windows `EPERM` startup failures, check `runtime.log` first and filter `scope == "pi-rpc"`. The complete log and state-file map lives in [build-release-observability.md](.agents/modules/build-release-observability.md#runtime-diagnostics).

## Harness

- 各功能模块保持故障隔离：单个模块初始化、运行或数据校验失败时，只降级并明确报告该模块，不得阻断其他模块或应用全局启动；仅当共享基础设施失效或无法维持基本一致性时，才允许升级为全局失败。
- 将既有 UI 的能力归属迁移到 Extension 时保持原有视觉、布局、文案和交互；除非用户明确要求重新设计，迁移只改变所有权和实现边界。
- 复合输入区域按能力边界隔离故障：附属控件失败时保留正文编辑与提交，富文本编辑器失败时降级为基础文本输入，避免用整块错误状态替换仍可工作的核心输入能力。
- Mortise 源码工具关闭时必须停止由它启动的源码桌面进程树，避免遗留前端、后台或已断开的日志管道。
- 权限审批能力由 Extension 提供，Mortise 核心只保留中立的扩展执行接口；权限状态不得通过额外前缀或动态提示注入模型上下文，产品权限模式只提供“询问”和“始终批准”。
- 权限 Extension 可以随 Mortise 分发为自带扩展，也可以独立安装；权限模式状态、策略判断、审批界面和工具调用决策必须完整实现在 Extension 中，Mortise 核心只提供不带权限语义的通用加载、事件、通信与持久化接口，不得保留权限专用实现或兼容执行分支。
- Mortise 默认信任所有扩展，不区分额外的扩展信任等级；核心按可自由修改和便利优先设计，安全隔离仅在明确提出时引入，相关讨论无需再用“可信扩展”作限定。
- 扩展 UI 采用由浅入深的声明模型：简单扩展仅提供少量语义参数并继承宿主默认值，高级扩展可在自身受控作用域内覆盖字体、间距、颜色等设计令牌；组合嵌套保持有限、可验证，不得通过全局样式或任意层级结构影响宿主及其他扩展。
- 扩展 UI 的响应式布局以核心聊天转录和输入框为最高优先级，不得挤占或破坏其可读性与可操作性；扩展可声明适配不同容量的紧凑表达，宿主在空间不足时按确定规则收纳为更多菜单、可展开区域或该区域适用的可滚动列表。
- 扩展接入规范以代码表达力、可组合性和工程清晰度为优先，不以无代码或小白式使用门槛为目标；应提供稳定的底层生命周期、挂载、通信和验收合同，让能够编程或借助 AI 编程的作者自由构建复杂 GUI。
- 扩展 GUI 能力验收必须覆盖对既有会话界面的实际介入、修改和卸载恢复；独立设置页或孤立面板只能作为补充证据，不能代替会话界面集成验证。
- CLI 相关变更必须检查 `apps/cli`、`scripts/mortise-ui` 与 Developer Kit 的同步影响：三者职责保持独立，但共享协议、输出合同、用户可见命令或验证能力发生变化时，应同步更新对应源码、文档、测试和版本化套件构建。
- 本项目的实际功能与界面验收必须使用项目自带 CLI，不得以通用浏览器控制或电脑控制能力替代；CLI 无法完成验收时，应修复或补齐 CLI 后再继续验收。
- Playground 作为 UI 快速查看和局部交互场，应尽可能收录所有可视 UI 组件及其关键状态或变体；允许使用局部模拟数据，但不得以此替代真实 AppShell 的完整界面验收。
- 新增或变更可独立呈现的可视 UI 时，必须同步登记 Playground 预览场景或受检查的非独立呈现豁免；动态场景使用确定性本地状态序列，并支持复现关键阶段。
- 隔离工作树或验收构建缺少外部工具链缓存时，先查找并复用本机其他 Mortise 工作树或已安装套件中同版本、同平台且可验证的缓存；复用时复制到当前隔离缓存目录，不直接共享可变构建输出，只有不存在可验证缓存时才联网下载。
- 已安装或随应用分发的扩展运行时代码必须在构建或安装阶段预编译为可验证的 JavaScript 产物；会话创建和首条请求只做动态资源发现、配置解析与运行时激活，只有开发态或临时发现的源码扩展可按内容缓存增量编译，不得对未变化源码按会话重复编译。
- 构建解耦先采用小范围、低风险的模块边界；新增或迁移的构建块必须显式声明输入、依赖、输出和可验证身份，并保持与未来增量缓存、隔离并发和最终组装兼容，不得把功能模块文档直接等同为构建单元。
- 打包默认复用最近一次已验证且身份完整的不可变构建；调用方无需传递构建编号才能获得复用，只有显式要求刷新源码身份时才执行源码捕获和冷构建判断。
- 长任务的通信状态与业务状态必须分离：通信超时或断线只表示结果待核实，不得自动判定业务失败或取消；长任务先持久化操作收据并返回 `operationId`，最终状态由领域事实源、状态查询和可恢复事件确认，真正取消仅来自用户显式取消或明确的资源回收逻辑。
- 各类会话中断后的继续能力统一复用“中断续接”协议：保留已持久化历史，闭合未完成工具为明确状态，并在新的 Attempt 中继续；协议本身不自动触发，重启、断线或运行失败后是否续接必须由用户或具体业务入口明确请求。
- 运行架构按 Workspace、Session、Attempt/Turn、Agent Loop、Provider 请求分层：Mortise 负责 Workspace、产品路由和投影，Pi 负责 Session 及以下的唯一状态机；`attemptId` 只用于事件归属和迟到隔离，不作为 Mortise 签发的执行权限，也不得在 Mortise 中维护一套竞争的 Attempt 状态。
