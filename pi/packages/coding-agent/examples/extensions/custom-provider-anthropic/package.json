{
  "name": "pi-extension-custom-provider-anthropic",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "clean": "echo 'nothing to clean'",
    "build": "echo 'nothing to build'",
    "check": "echo 'nothing to check'"
  },
  "pi": {
    "extensions": [
      {
        "id": "anthropic-provider-example",
        "path": "./index.ts",
        "targets": ["pi"],
        "manifest": {
          "schemaVersion": 1,
          "name": "Anthropic Provider Example",
          "version": "0.1.0",
          "author": { "name": "Mortise Contributors" },
          "engines": { "pi": "^0.1.0" },
          "capabilities": ["providers.anthropic"],
          "permissions": []
        }
      }
    ]
  },
  "dependencies": {
    "@anthropic-ai/sdk": "0.52.0"
  }
}
