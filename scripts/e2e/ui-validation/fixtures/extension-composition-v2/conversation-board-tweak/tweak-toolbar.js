export function mount(context) { const node = document.createElement('button'); node.type = 'button'; node.className = 'conversation-board-tweaked-toolbar'; node.dataset.mortiseSemanticId = 'conversation-board-tweak.toolbar'; node.textContent = 'Board++ compact'; node.addEventListener('click', () => context.notify('Third-level tweak command', 'success')); context.root.append(node); return () => node.remove() }
