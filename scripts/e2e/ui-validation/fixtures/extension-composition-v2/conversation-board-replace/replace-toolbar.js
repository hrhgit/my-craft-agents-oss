export function mount(context) { const node = document.createElement('button'); node.type = 'button'; node.className = 'conversation-board-replaced-toolbar'; node.dataset.mortiseSemanticId = 'conversation-board-replace.toolbar'; node.textContent = 'Board+ replaced'; node.addEventListener('click', () => context.notify('Replacement toolbar command', 'success')); context.root.append(node); return () => node.remove() }
